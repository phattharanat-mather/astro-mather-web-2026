---
// Switch variants by changing this single import.
// Available variants: hero-variants/HeroVariant1.astro
import Hero from '@/components/home/hero-variants/HeroVariant1.astro';
import type { HeroData } from '@/content-definition/home';

interface Props {
  data: HeroData;
}

const { data } = Astro.props;
---

<Hero data={data} />
